1. What is hoisting \*\*\*
2. Does let, const are also hoisted \*\*\*
3. Explain Temporal Dead Zone (TDZ) \*\*\*
4. Difference between == & === \*\*\*
5. What is sync code and async code \*\*\*
6. Explain event loop in browswer \*\*\*
7. Expliain starvation of function in callback queue \*\*
8. Explain proxy in object \*\*
9. Explain callback hell \*\*\*
10. Explain promises \*\*\*
11. Explain promise chaining \*\*\*
12. Explain all methods of promises \*\*\*
13. Explain async functions with await \*\*\*
14. Explain debouncing, throttling with example and real time usecase \*\*\*
15. Explain polyfills \*\*\*
16. Create polyfill for Promise.all, Promise.allsettled, map method, filter, includes, hasOwnProperty, find \*\*\*
17. Create a custom function for setInterval \*\*\*
18. What is a Variable Scope in JavaScript \*\*\*
19. Explain event delegation, event propogation, event bubbling, event capturing \*\*\*
20. Explain closure with example \*\*\*
21. Explain constructor function \*\*
22. Explain class in javascript \*\*
23. Explain oops concept with example and real time scenrios \*\*\*
24. Explain execution context and call stack \*\*\*
25. What is lexical scope and how js resolves variables lookups \*\*\*
26. Explain is 'this' keyword in javascript \*\*\*
27. What is prototype inheritence and how proto and prototype works (Expected : **proto** vs prototype, how object lookup works, how classes use prototype under the hood) \*\*
28. What shadowing and illegal shadowing \*\*
29. Explain currying \*\*\*
30. Difference between shallow copy and deep copy \*\*\*
31. What is a generator function \*\*
32. What is memoization? Imlement memoize function \*\*\*
33. Explain set with example and explain answer of this set.add([1]), set.add([1]) \*\*\*
34. Explain Object.defineProperty() (with get/set) \*\*
35. Explain optional chaining & Nullish Coalescing \*\*
36. Explain call, bind, apply \*\*\*
37. Difference b/w map, filter and reduce \*\*\*
38. How does garbage collection work in js \*\*\*
39. Difference between normal function and arrow function \*\*\*
40. What is reflow and repaint (DOM) \*\*\*
41. What is document fragment (DOM) \*\*\*
42. Difference between innerHTML vs textContent (DOM) \*\*\*
43. What is DOM Tree? What are Nodes (DOM) \*\*\*
44. What is event.stopPropogation(), event.stopImmediatePropagation(), event.preventDefault(). (DOM) \*\*\*
45. Difference between target and currentTarget.(DOM) \*\*\*
46. How browser renders a web page? (Critical Rendering Path) \*\*
47. Why is innerHTML unsafe? Explain XSS. (DOM) \*\*\*
48. What are the different data types in JavaScript?

49. What is the difference between primitive and non-primitive data types?
50. What is type coercion in JavaScript? Explain implicit and explicit type coercion with examples.
51. What is the difference between null, undefined, NaN, and not defined?
52. What are truthy and falsy values in JavaScript? Give examples.
53. What is the output of typeof null, typeof NaN, typeof [], typeof {}, and typeof function(){}? Explain why.
54. What is the difference between var, let, and const in terms of scope, redeclaration, and reassignment?
55. What is strict mode in JavaScript? What changes when "use strict" is enabled?
56. What is the difference between a function declaration and a function expression?
57. What are higher-order functions in JavaScript? Give a real-world example.
58. What are the differences between arrow functions and regular functions with respect to this, arguments, and constructor behavior?
59. What happens internally when the new keyword is used in JavaScript?
60. What is Object.create()? How is it different from creating an object using new?
61. What is the difference between **proto**, prototype, and Object.getPrototypeOf()?
62. Explain the prototype chain and how JavaScript performs property lookup.
63. How do JavaScript classes use prototypes internally?
64. What is destructuring in JavaScript? Explain array and object destructuring with examples.
65. What is the difference between the rest operator and spread operator?
66. What is the difference between Map and Object in JavaScript?
67. What is the difference between Set and Array? When would you use Set?
68. What are WeakMap and WeakSet? How are they different from Map and Set?
69. What are Symbols in JavaScript? What are their real-world use cases?
70. What are iterators in JavaScript? Explain the Symbol.iterator protocol.
71. What is the difference between map() and forEach()?
72. What is the difference between find() and filter()?
73. What is the difference between some() and every()?
74. What is the difference between slice() and splice()?
75. Explain findIndex(), includes(), concat(), and sort() with examples.
76. What is the difference between Object.keys(), Object.values(), and Object.entries()?
77. What is the difference between for...in and for...of?
78. How does error propagation work in a Promise chain?
79. What is Promise.finally()? When is it executed?
80. What is queueMicrotask()? How is it related to Promise microtasks?
81. What is event loop starvation? How can microtasks or long-running synchronous code cause starvation?
82. Predict the output of code containing setTimeout(), Promise.then(), queueMicrotask(), and async/await. Explain the execution order.
83. Explain the Node.js event loop and its different phases.
84. What is the difference between process.nextTick(), Promise microtasks, setImmediate(), and setTimeout() in Node.js?
85. What is the difference between CommonJS and ES Modules?
86. What is the difference between require()/module.exports and import/export?
87. What is EventEmitter in Node.js? How does it work?
88. What are Buffers in Node.js? Why are they required?
89. What are Streams in Node.js? Explain readable, writable, duplex, and transform streams.
90. What are Worker Threads in Node.js? When should you use them?
91. What are Child Processes in Node.js? What is the difference between exec(), spawn(), and fork()?
92. What is the Cluster module in Node.js? Why would you use it?
93. How does garbage collection work in JavaScript?
94. What is a memory leak in JavaScript? What are common causes of memory leaks?
95. How can closures cause memory leaks?
96. How can event listeners and timers cause memory leaks?
97. Explain event delegation with a dynamic list example.
98. What is the difference between preventDefault(), stopPropagation(), and stopImmediatePropagation()?
99. Explain event.target vs event.currentTarget with an example using event delegation.
100. Why can innerHTML be unsafe? Explain Cross-Site Scripting (XSS).
101. What is the difference between innerHTML, innerText, and textContent?
102. What is the difference between reflow and repaint?
103. How can you reduce unnecessary reflows and repaints in a web application?
104. Explain the Critical Rendering Path step by step.
105. What is the difference between synchronous and asynchronous event handlers in the browser?
106. What are the common output-based JavaScript interview questions involving hoisting, TDZ, closures, this, type coercion, and prototypes?
107. Predict the output of a program containing multiple Promise callbacks and setTimeout callbacks with different delays.
108. Write a function to reverse a string without using the built-in reverse() method.
109. Write a function to check whether a string is a palindrome.
110. Write a function to find duplicate elements in an array.
111. Write a function to remove duplicate elements from an array.
112. Write a function to find the frequency of each element in an array.
113. Write a function to find the maximum and minimum value in an array.
114. Write a function to find the second-largest number in an array.
115. Implement a deep equality function that compares nested objects and arrays.
116. Implement a retry mechanism for an asynchronous operation with a maximum number of retries.
117. Implement retry logic with exponential backoff for a failed API request.


JavaScript Interview Preparation
Synchronous & Asynchronous JavaScript — Core Concepts & Coding Practice

Section Count
Sync JS — Theory Questions 10
Async JS — Theory Questions 10
Sync JS — Coding Questions 20
Async JS — Coding Questions 20


Part 1: Synchronous JavaScript — Theory Questions (10)

1. What does it mean for JavaScript to be a single-threaded, synchronous language by default?
2. Explain the Call Stack. How does JavaScript use it to execute code?
3. What is hoisting? How do var, let, and const behave differently when hoisted?
4. What is the Temporal Dead Zone (TDZ)?
5. Explain the difference between function scope and block scope with examples.
6. What is a closure? Why are closures important in JavaScript?
7. Explain how the 'this' keyword behaves in a regular function vs an arrow function vs a method.
8. What is the difference between call(), apply(), and bind()?
9. What is the difference between primitive and reference data types, and how does that affect equality
checks and passing values to functions?
10. Explain prototypal inheritance. How is it different from classical (class-based) inheritance?

Part 2: Asynchronous JavaScript — Theory Questions (10)

1. What does it mean for JavaScript to be 'asynchronous' if it's single-threaded? Explain with the help of
the event loop.
2. Explain the Call Stack, Web APIs (or Node APIs), Callback Queue (Task Queue), and Microtask
Queue, and how they interact.
3. What is the difference between the Microtask Queue and the Macrotask (Callback) Queue? Give
examples of what goes into each.
4. Why does a Promise.then() callback run before a setTimeout(fn, 0) callback?
5. What is a callback function? What is 'callback hell' and how do Promises solve it?
6. Explain the three states of a Promise: pending, fulfilled, and rejected.
7. What is the difference between Promise.all(), Promise.race(), Promise.allSettled(), and Promise.any()?
8. How does async/await work under the hood? Is it syntactic sugar over Promises?
9. How do you handle errors in async/await code vs in Promise chains?
10. What is the difference between setTimeout, setInterval, and requestAnimationFrame? What are the
pitfalls of setInterval?
Part 3: Synchronous JavaScript — Coding Questions (20)
1. Predict the output: console.log(a) followed by var a = 5; then console.log(b) followed by let b = 10;
Explain why the two behave differently.
2. Write a function makeCounter() that returns an object with increment(), decrement(), and getValue()
methods using a closure to keep the count private.
3. Fix the classic loop-closure bug: a for loop using var that logs the wrong value inside a function created
in each iteration. Show the fix using let and again using an IIFE.
4. Write a function once(fn) that ensures a given function fn can only be called one time; subsequent calls
should return the result of the first call without re-executing fn.
5. Implement your own version of Array.prototype.map called myMap, without using the built-in map.
6. Implement your own version of Array.prototype.filter called myFilter, without using the built-in filter.
7. Implement your own version of Array.prototype.reduce called myReduce, without using the built-in
reduce.
8. Write a function memoize(fn) that caches the results of expensive function calls based on their
arguments.
9. Explain and fix the output of an object method that loses its 'this' context when passed as a standalone
callback (e.g., passed to setTimeout or used as an event handler).
10. Implement a curry(fn) function that transforms a function of multiple arguments into a sequence of
functions each taking a single argument.
11. Write a function deepClone(obj) that creates a deep copy of a nested object or array without using
JSON.parse/JSON.stringify.
12. Write a function flattenArray(arr) that flattens a deeply nested array into a single-level array, without
using Array.prototype.flat().
13. Implement a simple pub-sub (Observer) pattern with subscribe(event, callback), unsubscribe(event,
callback), and publish(event, data) methods.
14. Write a function debounce(fn, delay) from scratch and explain a real use case for it (e.g., a search
input).
15. Write a function throttle(fn, limit) from scratch and explain how it differs from debounce.
16. Implement your own bind() method as Function.prototype.myBind, without using the built-in bind.
17. Write a function that checks whether two values are deeply equal (deepEqual(a, b)), handling nested
objects and arrays.
18. Implement a simple LRU (Least Recently Used) cache class with get(key) and put(key, value)
methods using a Map.
19. Write a function compose(...fns) and a function pipe(...fns) that combine multiple functions into one,
and explain the difference between them.
20. Predict the output of a snippet involving object property shorthand, destructuring with default values,
and the spread/rest operators together, then explain each line.


Part 4: Asynchronous JavaScript — Coding Questions (20)

1. Predict the output: a script that logs '1', schedules setTimeout(() => log('2'), 0), then
Promise.resolve().then(() => log('3')), then logs '4'. Explain the exact order and why.
2. Write a function delay(ms) that returns a Promise which resolves after ms milliseconds, and use it to log
'Hello' after 1 second and 'World' one second after that.
3. Implement your own simplified version of Promise.all() called myPromiseAll(promises).
4. Implement your own simplified version of Promise.race() called myPromiseRace(promises).
5. Implement your own simplified version of Promise.allSettled() called myPromiseAllSettled(promises).
6. Debug this async function: it calls fetch(url) and res.json() without using await before returning data. Fix
it and explain what would actually be returned without the fix.
7. Write an async function that fetches data from 3 different URLs sequentially (one after another), then
rewrite it to fetch all 3 in parallel using Promise.all, and explain the performance difference.
8. Convert a chain of 3 nested setTimeout calls (callback hell style) into a Promise-based chain, and then
into an async/await version.
9. Write an async function fetchWithRetry(url, retries) that retries a failed network request up to a given
number of times before giving up.
10. Write an async function fetchWithTimeout(url, ms) that rejects if the request takes longer than ms
milliseconds, using Promise.race.
11. Implement a function asyncSeries(tasks) that runs an array of async functions one after another and
collects their results in order.
12. Implement a function asyncParallelLimit(tasks, limit) that runs an array of async tasks with a maximum
of `limit` tasks running concurrently at any time.
13. Predict the output of a script mixing multiple setTimeout calls with different delays and multiple chained
.then() Promise callbacks, and explain the full execution order step by step.
14. Write a custom Promise-like class from scratch (MyPromise) that implements resolve, reject, and a
basic then() method.
15. Explain and demonstrate with code why 'await' inside a forEach loop does not work as expected, and
show the correct way to run async operations for each item in an array sequentially, and then in parallel.
16. Write an async generator function that yields values one at a time with a delay between each (e.g.,
using setTimeout wrapped in a Promise), and consume it with a for-await-of loop.
17. Implement a simple event-driven task queue that processes async jobs one at a time, ensuring the
next job only starts after the previous one resolves.
18. Write a function promisify(fn) that converts a traditional Node-style callback function (fn(args...,
callback)) into a function that returns a Promise.
19. Predict the output: an async function that has a try/catch block, where an awaited Promise rejects —
explain exactly how and when the catch block executes relative to other synchronous code after the
function call.
20. Design (in code) a simple caching layer for API calls using async/await, so that if the same request is
made again while the first one is still pending, both callers receive the same in-flight Promise instead of
firing a duplicate network request.